Options -MultiViews
RewriteEngine On

# Serve real frontend files and directories normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React Router handles every other URL in the browser.
RewriteRule ^ index.html [L]
