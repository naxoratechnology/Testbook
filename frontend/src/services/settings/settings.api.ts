import axios from 'axios'; import * as yup from 'yup';
export interface AdminSettingsData { platformName: string; supportEmail: string; defaultExam: string; aboutTitle: string; aboutDescription: string; contactEmail: string; contactPhone: string; contactAddress: string; mediaStorage: 'Cloudinary'; autoNotify: boolean; allowSyllabusDownload: boolean; blockPdfDownloads: boolean }
export const settingsSchema = yup.object({ platformName: yup.string().trim().required(), supportEmail: yup.string().trim().email().required(), defaultExam: yup.string().trim().required(), aboutTitle: yup.string().trim().required(), aboutDescription: yup.string().trim().required(), contactEmail: yup.string().trim().email().required(), contactPhone: yup.string().trim().default(''), contactAddress: yup.string().trim().default(''), mediaStorage: yup.mixed<'Cloudinary'>().oneOf(['Cloudinary']).required(), autoNotify: yup.boolean().required(), allowSyllabusDownload: yup.boolean().required(), blockPdfDownloads: yup.boolean().oneOf([true]).required() });
const api = axios.create({ baseURL: import.meta.env.VITE_API_URL, withCredentials: true }); export const settingsApiService = { get: () => api.get('/settings'), update: (payload: AdminSettingsData) => api.patch('/settings', payload) };
