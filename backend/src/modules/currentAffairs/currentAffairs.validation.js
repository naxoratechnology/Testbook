function validate(body = {}) { const value = { date: body.date, title: String(body.title || '').trim(), exam: String(body.exam || 'All Exams'), highlights: Array.isArray(body.highlights) ? body.highlights : String(body.highlights || '').split('\\n').map((x) => x.trim()).filter(Boolean), questions: Array.isArray(body.questions) ? body.questions : [], status: body.status || 'draft' }; const errors = {}; if (!value.date) errors.date = 'Date is required.'; if (!value.title) errors.title = 'Title is required.'; return { value, errors }; } module.exports = { validate };
