function validate(body = {}) { const value = { title: String(body.title || '').trim(), message: String(body.message || '').trim(), type: body.type || 'announcement', href: String(body.href || ''), audience: body.audience === 'selected' ? 'selected' : 'all', recipients: Array.isArray(body.recipients) ? body.recipients : [] }; const errors = {}; if (!value.title) errors.title = 'Title is required.'; if (!value.message) errors.message = 'Message is required.'; if (value.audience === 'selected' && !value.recipients.length) errors.recipients = 'At least one recipient is required.'; return { value, errors }; } module.exports = { validate };
