function validate(body = {}) { const value = { name: String(body.name || '').trim(), description: String(body.description || '').trim(), exam: String(body.exam || '').trim(), subject: String(body.subject || '').trim(), status: ['draft', 'published', 'unpublished'].includes(body.status) ? body.status : 'draft' }; const errors = {}; ['name', 'exam', 'subject'].forEach((key) => { if (!value[key]) errors[key] = key + ' is required.'; }); if (value.subject.length > 120) errors.subject = 'Subject folder must be 120 characters or fewer.'; return { value, errors }; }
module.exports = { validate };
